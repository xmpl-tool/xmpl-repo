#generate a certificate signing request (csr) for an existing private key
#generate certificate signing request csr existing private key
openssl req -out {:output csrequest (.csr):} -key {:input privateKey (.key):} -new
