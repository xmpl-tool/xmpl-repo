#resynchronize the package index files from their repository sources on debian based distributions
#apt update sync synchronize repo source sources list ubuntu mint package manager
#may need root permissions
apt-get update
